/**
 * Intentionally-empty right sidebar for the Earn page. Reserves the sidebar
 * column (so the main content keeps its width) without rendering activity yet.
 * Replace with real content when we decide what belongs here.
 */
export function EarnSidebarPlaceholder() {
  return <div className="w-full" aria-hidden="true" />;
}
